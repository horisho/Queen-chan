########## Direct application dependencies ##########
# discord.py: Discord bot framework (includes aiohttp and its transitive deps)
discord.py~=2.5.0
# python-dotenv: Load .env for token & config
python-dotenv>=1.0,<2.0
# python-chess library (distribution name is 'chess')
chess>=1.11,<2.0
# CairoSVG for converting SVG (from python-chess) to PNG
cairosvg>=2.7,<3.0
# Pillow for image creation / manipulation
pillow>=11.0,<12.0

########## (Optional) If font rendering needs system libs ##########
# On some systems you may need one of the following installed via OS packages:
#   apt-get install libcairo2 libpango-1.0-0 libffi-dev

########## Developer / tooling (uncomment if needed) ##########
# pip-tools
# pip-tools>=7.0
# mypy (type checking)
# mypy>=1.10

########## Notes ##########
# - Removed transitive / unused packages (Flask, waitress, joblib, etc.).
# - If you reintroduce a keep-alive web server, add: Flask, waitress.
# - Run `pip install -r requirements.txt` after pruning the old environment.
